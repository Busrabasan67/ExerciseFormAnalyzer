<resources>
    <!-- Uygulamanın teması — Material3 ile tam ekran kamera görünümü için -->
    <style name="Theme.ExerciseFormAnalyzer" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>
</resources>
